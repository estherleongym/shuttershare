<% if logged_in? %>

<%= link_to 'Edit Profile', edit_profile_path(current_user.id) %>
<%= link_to 'View Profile', user_photos_path(current_user.id) %>
<%= link_to 'Logout', session_path(current_user.id), method: :delete %>

<% else %>

<%= render partial: '/sessions/new' %>

<% end %>
