<h1>Edit Photo</h1>

<%= form_for @photo, url:"/users/#{@user_id}/photos/#{@photo_id}", :html => {:multipart => true} do |f| %>
    <p>Caption<%= f.text_field :caption %></p>
    <p><%= f.submit %></p>
<% end %>
